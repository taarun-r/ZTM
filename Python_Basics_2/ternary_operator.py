# Ternary Operator - Same as if and elif and else statement, and is a short cut
# AKA conditional expressions
#  There are special cases where you can use a ternary operator
# Syntax: condition_if_true if condition else condition_if_else

is_friend = True
can_message = "message allowed" if is_friend else "not allowed to message"

print(can_message)
